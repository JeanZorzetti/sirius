ROLE: Senior UI/UX Designer (Specialist in Modern SaaS Aesthetics like Linear, Raycast).

OBJECTIVE:
The user is UNHAPPY with the current Light Mode. It feels "washed out" and "didn't change anything".
We need a **RADICAL** improvement in Light Mode Contrast and Texture.

REQUIREMENTS:

1.  **Texture Pattern (Crucial):**
    *   Add a class `.light-pattern` (or apply to `body:not(.dark)`) with a **subtle but visible** background pattern.
    *   Suggestion: `radial-gradient` dots (grey) on a light gray background. This adds depth so it's not just "flat white".
    *   CSS: `background-image: radial-gradient(#e5e7eb 1px, transparent 1px); background-size: 24px 24px;` (Adjust colors to be visible).

2.  **High Contrast Separation:**
    *   **Background:** Must be darker (`zinc-100` / `#f4f4f5`) to make the White Cards POP.
    *   **Cards:** Pure White (`#ffffff`) with a **crisp border** (`zinc-200`) and **shadow** (`shadow-sm`).
    *   **Text:**
        *   Headers: Pure Black (`#000000`).
        *   Body: Dark Gray (`zinc-700` / `#3f3f46`).
        *   Muted: Mid Gray (`zinc-500` / `#71717a`).

3.  **Sidebar (Light Mode):**
    *   Should be distinct from the main content. Maybe `zinc-50` border-r `zinc-200`.

INPUT FILE: `app/globals.css`.
RETURN UPDATED CSS with the new Pattern and High Contrast variables.

=== app/globals.css (CURRENT) ===
... [I will provide content] ...
