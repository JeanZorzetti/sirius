Estou recebendo este erro no Vercel/Easypanel mesmo após rodar "prisma migrate deploy" no build:
"Erro interno: Invalid `prisma.organization.create()` invocation: The column `plan` does not exist in the current database."

Contexto:
- Usamos Prisma com PostgreSQL.
- O schema.prisma tem a coluna `plan String @default("FREE")`.
- O script de build é: `npx prisma migrate deploy && npx prisma generate && next build`.
- O log do deploy diz "No pending migrations to apply", o que é estranho se a coluna não existe.

Hipótese:
Pode ser que o arquivo de migração SQL físico não tenha sido criado/commitado, então o `migrate deploy` acha que está tudo certo (baseado no histórico) mas o banco real não tem a coluna.

Tarefa:
1. Verifique se existe um arquivo .sql na pasta `prisma/migrations` que adiciona a coluna `plan`.
2. Se não existir, me diga o comando exato para criar essa migration LOCALMENTE para que eu possa commitar.
