The user wants to improve the `AnalyticsPage` to match the promised features:
1.  **Conversion Rate** ("Taxa de conversão")
2.  **Average Ticket** ("Ticket médio" - already exists, but ensure it's prominent)
3.  **Closing Forecast** ("Previsão de fechamento")

**Changes to `app/dashboard/analytics/page.tsx`**:

1.  **Metrics Calculation**:
    *   **Forecast (Previsão)**:
        *   Filter deals where `closeDate` is in the *current month* (or future).
        *   Sum their values.
        *   Display this as "Previsão de Fechamento (Este Mês)".
    *   **Conversion Rate**:
        *   Since we don't have explicit "WON" stage type in schema yet, let's calculate simplistic "Conversion" as `(Deals in last stage) / (Total Deals)`.
        *   *Better approach*: Check for stage names containing "Ganho", "Vendido", "Fechado", or "Won". If none, use the last stage order.
        *   Calculate percentage.

2.  **UI Updates**:
    *   **Cards**:
        *   Keep **Total Value**.
        *   Keep **Average Ticket**.
        *   Replace "Total Deals" with **Conversion Rate** (or add as 4th card?). Let's keep 3-4 cards. Maybe 4 cards grid.
        *   Add **Forecast** card.
    *   **Visuals**:
        *   Use `Lucide` icons: `Target` (Conversion), `CalendarClock` (Forecast).
        *   Keep the premium glassmorphism style.

**Code Logic**:
```typescript
const now = new Date();
const currentMonth = now.getMonth();
const currentYear = now.getFullYear();

const forecastDeals = deals.filter(deal => {
    if (!deal.closeDate) return false;
    const d = new Date(deal.closeDate);
    return d.getMonth() === currentMonth && d.getFullYear() === currentYear;
});
const forecastValue = forecastDeals.reduce((sum, deal) => sum + Number(deal.value || 0), 0);

// Conversion (heuristic: deals in stages named 'Won'/'Ganho' or last stage)
// Sort stages by order first
// Find 'won' stage
```
