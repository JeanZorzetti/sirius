The user wants to add the **WhatsApp Button** to the `CreateDealDialog` as well (it's already in `EditDealDialog`).

**Target File**: `components/deals/create-deal-dialog.tsx`

**Required Changes**:
1.  **Update `Contact` Type**:
    *   Add `phone?: string | null` to the `Contact` type definition (around line 32).
2.  **Add State**:
    *   Add `const [selectedContactId, setSelectedContactId] = useState<string>('')`.
3.  **Update Select Component**:
    *   Add `onValueChange={setSelectedContactId}` to the `Select` component for `contactId`.
    *   (Optional) Make it controlled `value={selectedContactId}` if needed, but `onValueChange` is enough to track it while keeping `name="contactId"` for the form data. Actually, using `name` works with uncontrolled, but `onValueChange` works with uncontrolled root? Yes, `Select` from shadcn works this way.
4.  **Add Buffer/UI**:
    *   Display the **WhatsApp Button** next to the Contact select (or in a logical spot nearby), *only* if `selectedContactId` corresponds to a contact with a phone number.
    *   Use the same styling as `EditDealDialog` (Green button, `MessageCircle` icon).

**Code Snippet Reference**:
```tsx
const selectedContact = contacts.find(c => c.id === selectedContactId)

{selectedContact?.phone && (
    <Button
        type="button"
        variant="outline"
        size="icon"
        className="ml-2 shrink-0 text-green-600 border-green-200 hover:bg-green-50 ..."
        onClick={() => ...}
    >
        <MessageCircle className="w-4 h-4" />
    </Button>
)}
```
