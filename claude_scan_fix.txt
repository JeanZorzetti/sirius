You are the Lead Developer. I am the CTO.

We are still seeing this error in the logs:
```
Error: Deal not found
    at u (.next/server/chunks/ssr/[root-of-the-server]__4d51215d._.js:2:5140)
    at async o (.next/server/chunks/ssr/_afaa0474._.js:1:7620)
    ...
    digest: '3776837303'
```

Please perform a scan of the relevant files, specifically:
- `app/dashboard/deals/actions.ts`
- `components/deals/edit-deal-dialog.tsx`

The goal is to RESOLVE this problem.
1. Check if the "Deal not found" error is being thrown too aggressively or if the client handling isn't working.
2. If we are throwing an error for a "not found" state, consider returning `{ error: "Not found" }` or similar instead of throwing, so we don't spam the server logs with 500s.
3. Ensure the UI handles this gracefully (closes modal, shows toast).

You have permission to edit the files. Fix the issue.
