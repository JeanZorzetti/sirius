ROLE: Senior Frontend Designer (Dark Mode Specialist).

OBJECTIVE:
Execute "Phase 1: Foundation" of our UX Roadmap.
Refactor `app/globals.css` and `components/dashboard/sidebar.tsx` to MATCH the premium design language of `FeatureGateModal`.

REFERENCE DESIGN (`feature-gate-modal.tsx`):
- Colors: Zinc 950 (Base), Indigo 950 (Accents), Indigo 500 (Glows).
- Style: Glassmorphism, 1px borders (white/10), Gradients.
- Typography: Clean Sans (Inter/Geist).

TASKS:

1. **Refactor `app/globals.css`**:
   - Change the `:root` and `.dark` variables.
   - Set the DEFAULT background to a "Midnight" dark (e.g., `#09090b` or match the modal's `zinc-950`).
   - Define `--sidebar-background` to match the modal's deep theme (not white/gray).
   - Add utility classes for "glass-panel" if helpful.

2. **Refactor `components/dashboard/sidebar.tsx`**:
   - **Visuals:** Remove the solid white/gray look. Make it look like a "Floating Glass Panel" or at least a dark premium sidebar.
   - **Logo:** Add the "Crown" icon gradient style to the SIRIUS logo.
   - **Navigation:**
     - Active Item: Glow effect (e.g., `bg-indigo-500/10 text-indigo-400 border-l-2 border-indigo-500`).
     - Inactive Item: `text-zinc-500 hover:text-zinc-300`.
   - **Layout:** Ensure it sits nicely in the new dark theme.

INPUT FILES PROVIDED BELOW.
RETURN THE UPDATED CODE FOR BOTH FILES.

=== components/dashboard/billing/feature-gate-modal.tsx (REFERENCE) ===
[... Reference code from previous turns, I will paste it physically in the pipe ...]

=== app/globals.css (CURRENT) ===
@import "tailwindcss";
@import "tw-animate-css";
... [current content] ...

=== components/dashboard/sidebar.tsx (CURRENT) ===
... [current content] ...
