The user executed a cleanup of Deals, and now their Pipeline view is empty.
They suspect the DB structure is wrong ("Pipeline shouldn't fail if Deals are missing").
Reality: The `PipelineStage` records probably didn't exist for their correct Organization ID (due to the previous bug), or were deleted somehow.

Task:
1.  **Verify Schema**: Confirm `PipelineStage` is a parent of `Deal` and can exist without Deals. (It is).
2.  **Create Fix Script**: Create `scripts/fix-org-stages.ts` (or reuse `seed-data.ts` but make it broader).
    - Logic: Find ALL Organizations.
    - For each Org, check if it has `PipelineStage`s.
    - If 0 stages, create the defaults: 'Prospecção' (1), 'Qualificação' (2), 'Proposta' (3), 'Fechamento' (4).
    - Ensure correct `organizationId`.
3.  **Run the Script**: Execute it with `npx tsx scripts/fix-org-stages.ts`.
4.  **Explain**: Output a message confirming that Stages were restored and are independent of Deals.

Note: Use `prisma` client.
