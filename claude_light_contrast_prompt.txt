ROLE: Senior Accessibility Engineer & Visual Designer.

OBJECTIVE:
Refactor the **Light Mode** palette in `app/globals.css` to achieve **High Contrast (WCAG AA/AAA)**.
The previous iteration was too "soft" or "washed out". We need sharp, professional contrast.

REQUIREMENTS:
1.  **Text Contrast:**
    *   `foreground`: Use almost pure black (`zinc-950` / `oklch(0.13 0.02 260)`).
    *   `muted-foreground`: MUST be readable. Use a darker gray (`zinc-600` / `oklch(0.45 0 0)` instead of `zinc-400`).
2.  **Borders:**
    *   `border`: Increase visibility. Use `zinc-300` (`oklch(0.88 0 0)`) or `zinc-200` with high opacity. Borders should be crisp.
3.  **Backgrounds:**
    *   Keep `background` as `zinc-50` (`#f8fafc`).
    *   Keep `card` as `white`.
4.  **Primary:**
    *   Ensure the Indigo accent survives the light background. Darken it slightly if needed.

INPUT FILE PROVIDED BELOW.
RETURN UPDATED `:root` BLOCK FOR `app/globals.css`.

=== app/globals.css (CURRENT) ===
... [I will provide content] ...
