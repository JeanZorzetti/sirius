ROLE: Senior Frontend Designer (Animation & Polish).

OBJECTIVE:
Execute "Phase 3: Polish" of our UX Roadmap.
Refactor `app/dashboard/loading.tsx` and enhance `components/kanban-board.tsx` to add Premium Loading States and Empty States.

CONTEXT:
The app is now "Midnight Indigo" (Dark Premium).
Users hate generic spinners. We need "Skeletons" that perfectly match the new Kanban layout.

TASKS:

1. **Refactor `app/dashboard/loading.tsx`:**
   - Remove the generic `Loader2` spinner.
   - Create a **Premium Skeleton Screen** that mimics the Kanban Board.
   - Show 3-4 columns (`bg-white/5` rounded-t-2xl).
   - Show "Ghost Cards" inside the columns (`rounded-xl bg-[#121217] h-32 animate-pulse`).
   - Use `h-screen w-full` to prevent layout shifts.

2. **Enhance `components/kanban-board.tsx` (Empty State):**
   - Create a sub-component `EmptyColumnState` inside this file.
   - Replace the text "Arraste items aqui" with a beautiful SVG/Icon illustration (e.g., a dashed box with a faded "Ghost Card" icon).
   - Style: `border-2 border-dashed border-white/5 bg-white/[0.01] rounded-xl flex items-center justify-center text-zinc-600`.

INPUT FILES PROVIDED BELOW.
RETURN THE UPDATED CODE FOR BOTH FILES.

=== app/globals.css (REFERENCE) ===
@import "tailwindcss";
... [I will provide the file content] ...

=== app/dashboard/loading.tsx (CURRENT) ===
... [I will provide the file content] ...

=== components/kanban-board.tsx (CURRENT) ===
... [I will provide the file content] ...
