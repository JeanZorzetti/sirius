You are the Lead Developer. I am the CTO.
We have a 500 Error "Deal not found" when opening the Edit Deal Dialog.

CONTEXT:
Error Log:
POST https://sirius.roilabs.com.br/dashboard 500 (Internal Server Error)
Error: Deal not found

FILES TO MODIFY:
1. `app/dashboard/deals/actions.ts`
2. `components/deals/edit-deal-dialog.tsx`

INSTRUCTIONS:
1. In `app/dashboard/deals/actions.ts`:
   - Locate `getDealDetails`.
   - Add `console.log` before fetching to show `dealId` and current user info.
   - Add `console.log` after fetching to show if `deal` was found or null.
   - Keep the error throw, but ensure we have logs.

2. In `components/deals/edit-deal-dialog.tsx`:
   - Locate the `useEffect` that calls `getDealDetails`.
   - Improve the `.catch` block.
   - If an error occurs:
     - Log it.
     - Show a reliable alert/toast (e.g., `alert("Deal não encontrado ou você não tem acesso.")`).
     - Call `onOpenChange(false)` to close the modal immediately so the user isn't stuck.

OUTPUT:
Please provide the full corrected code for both files.
