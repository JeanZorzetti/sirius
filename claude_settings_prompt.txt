ROLE: Senior Frontend Engineer / UI Designer.

OBJECTIVE:
Refactor the "Settings" page and "Profile Form" to match the project's new "Premium Glass" and "High Contrast" aesthetic.

CONTEXT:
We have recently overhauled the CRM's UI. 
-   **Dark Mode**: Deep Slate background, Glass cards (`bg-white/[0.02]`), Indigo accents.
-   **Light Mode**: Gray background (`zinc-100`), White cards (`bg-white`), Crisp borders (`zinc-300`), Deep Black text.
-   **Typography**: Uppercase Page Titles (e.g., "CONTATOS"), Zinc-900/White headers.

TASKS:

1.  **Refactor `app/dashboard/settings/page.tsx`**:
    *   **Header**: Change title to "CONFIGURAÇÕES" (uppercase, bold). Add a subtitle. Use the same styling as Contacts/Analytics pages.
    *   **Layout**: Use a nice container with padding.
    *   **Card**: Wrap the Profile section in a **Premium Card**:
        *   Class: `bg-white dark:bg-white/[0.02] border-zinc-200 dark:border-white/5 backdrop-blur-xl shadow-sm rounded-xl`.
    *   **Icons**: Add a nice Lucide icon to the Card Header (e.g., `User` with a gradient background/badge).

2.  **Refactor `components/settings/profile-form.tsx`**:
    *   **Styling**: Ensure standard `shadcn/ui` components `Input`, `Label`, `Button` are used (they are already set up correctly in `globals.css`).
    *   **Feedback**: Improve the "Success/Error" alert. Instead of `bg-green-50`, use a cleaner look or a specific shadcn Alert component logic (or just nice Tailwind classes like `bg-emerald-500/10 text-emerald-600 dark:text-emerald-400 border border-emerald-500/20 rounded-lg p-3`).
    *   **Buttons**: Use `variant="default"` (Indigo).

INPUT FILES:
`app/dashboard/settings/page.tsx`
`components/settings/profile-form.tsx`

OUTPUT FORMAT:
Provide the FULL content for both files.
