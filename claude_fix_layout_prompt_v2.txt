O problema persiste: O botão de upgrade (branco) ainda está cortado na tela do usuário.
Parece que o modal ainda está muito alto para a viewport dele.

TAREFA CRÍTICA:
Refatore o Layout para ser ULTRA COMPACTO e garantir que a rolagem funcione corretamente.

1. REMOVA paddings desnecessários. O Header deve ser bem mais curto.
2. Tamanho dos ícones: Reduza.
3. Estrutura de Scroll:
   - `DialogContent` deve ter altura fixa ou max-height.
   - O Header deve ser fixo (shrink-0).
   - A lista de features deve ser a ÚNICA coisa a rolar (flex-1 overflow-y-auto).
   - O Footer deve ser fixo no fundo (shrink-0), para que os botões SEMPRE apareçam, mesmo que a lista de features precise de scroll.

Isso é essencial: O Footer NÃO pode rolar junto com o conteúdo. Ele deve estar sempre visível "stickado" no fundo do modal, enquanto a lista de features rola se não houver espaço.

Código Atual:
'''tsx
[Código atual do arquivo components/dashboard/billing/feature-gate-modal.tsx]
'''

Retorne APENAS o código corrigido.
