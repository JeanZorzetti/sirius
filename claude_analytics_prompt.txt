ROLE: Senior Data Visualization Designer (Recharts Specialist).

OBJECTIVE:
Refactor `components/analytics/overview-chart.tsx` and `app/dashboard/analytics/page.tsx` to match the new "Deep Slate" High Contrast theme.
The current chart is standard/boring. We need a **"Premium Dashboard Widget"**.

CONTEXT:
Theme: High Contrast Dark Mode (`bg-[#2a2d36]`, `text-white`).
Accent: Indigo (`#6366f1`).
Library: `recharts` (ResponsiveContainer, BarChart, etc).

TASKS:

1. **Refactor `components/analytics/overview-chart.tsx`:**
   - **Gradient Bars:** Use `<defs>` to create a vertical gradient for the bars (Indigo to Purple).
   - **Radius:** Rounded top corners for bars (`radius={[4, 4, 0, 0]}`).
   - **Grid:** Subtle dashed grid lines (`strokeDasharray="3 3" strike="rgba(255,255,255,0.1)"`).
   - **Tooltip:** Custom premium tooltip (Glass background, dark border, bright text).
   - **XAxis/YAxis:** Lighter text color (`gray-400` equivalent) for readability.

2. **Enhance `app/dashboard/analytics/page.tsx`:**
   - **KPI Cards:** Update to use "Glass Card" styling (`bg-white/[0.02] border-white/5`).
   - **Layout:** Ensure nice spacing and hierarchy.

INPUT FILES PROVIDED BELOW.
RETURN UPDATED CODE FOR BOTH FILES.

=== components/analytics/overview-chart.tsx (CURRENT) ===
... [I will provide content] ...

=== app/dashboard/analytics/page.tsx (CURRENT) ===
... [I will provide content] ...
